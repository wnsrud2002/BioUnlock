.build/
build/
.DS_Store
*.xcuserdatad
Debug_Captures/

# 모델 변환용 파이썬 가상환경 — 946MB, tools/setup 스크립트로 재현 가능
tools/.venv/

# 변환 원본 ONNX — 라이선스 재확인 없이 커밋하면 안 되는 3rd party 파일들.
# tools/convert_*.py 가 각자 원본 URL 에서 다시 받는다.
*.onnx

# 안티스푸핑 모델 변환용으로 clone 한 저장소. 자체 .git 이 있어 중첩 저장소가 된다.
# tools/setup_antispoof_source.sh 로 다시 clone 한다.
tools/sf/
