# 엔지니어링 기록

BioUnlock(전신: Unlockface)을 만들면서 실제로 겪은 문제와 근거를 남긴다.
"그럴듯하게 만들었다"가 아니라 각 단계에서 무엇을 실측했고, 어떤 가정이
틀렸었고, 어떻게 검증했는지를 숫자로 남기는 게 목적이다. 마케팅 문구가
아니라 디버깅 로그에 가깝다.

## 왜 만들었나

macOS에는 Windows Hello 같은 표준화된 서드파티 얼굴 인식 언락이 없다.
Apple Face ID는 Secure Enclave가 있는 기기 전용이고, 일반 Mac 카메라로는
쓸 수 없다. 있는 것을 확인하고 없길래 직접 만들었다.

## 근본적인 제약: macOS는 진짜 생체인증을 허용하지 않는다

macOS는 서드파티가 로그인/잠금화면 인증 경로에 끼어드는 것을 SIP로 막는다.
그래서 이 앱은 "생체인증"이 아니라, **얼굴이 일치하면 키체인에 암호화해 둔
로그인 비밀번호를 가상 키보드로 대신 입력하는 자동화**다. 이 사실이 이후
모든 설계 결정(비밀번호를 어떻게 안전하게 보관할지, 잘못된 순간에 잘못된
곳에 타이핑되지 않게 어떻게 막을지)의 출발점이다.

---

## Phase 1 — 카메라 + 얼굴 검출: Vision의 자세 각도가 못 쓸 정도로 양자화돼 있었다

`VNFaceObservation.yaw/pitch/roll`을 쓰면 될 줄 알았다. 실측해보니:

```
13:16:51.772 yaw=0.000000 pitch=nil roll=-0.523599
13:16:52.768 yaw=0.000000 pitch=nil roll=+0.523599
```

- `pitch`는 `VNDetectFaceLandmarksRequestRevision3`에서 **항상 nil**이다.
- `roll`/`yaw`는 π/6, π/4 단위로 **양자화**돼 있다. 등록 포즈 버킷 판정 임계값이
  0.10 rad(5.7°) 수준이라 이 해상도로는 아무것도 판정할 수 없었다.

랜드마크(5점)에서 강체 머리 모델로 자세를 직접 계산하는 쪽으로 바꿨다.
처음엔 눈선의 기울기를 roll로 썼는데, 실측에서 yaw 회전이 roll을 오염시키는
걸 발견했다(상관계수 -0.983). 눈중심→입중심 벡터를 축으로 바꾸자 오염이
사라졌다(회전 중 roll 오차 36° → 3.6°).

각도 게인(`noseDepthRatio`)도 추정이 두 번 틀렸다. 처음엔 큰 회전에서 콧방울을
코끝으로 잘못 집어(각도가 커지면 정상적인 코끝 후보가 필터 범위를 벗어나
버그가 있는 콧방울 후보로 대체됨) 0.48로 나왔는데, `nose` 랜드마크와
`medianLine`(얼굴 중심선) 랜드마크의 교집합으로 코끝을 다시 특정하자 0.22로
정정됐고 좌우 대칭성도 살아났다(좌 0.223 / 우 0.219 vs 이전 좌 0.565 / 우 0.413).

## Phase 2 — 정렬: 유사변환, 실측 기반 게이트

ArcFace 표준 5점 정준 좌표로 유사변환(회전+등방스케일+평행이동, 4자유도)
정렬을 한다. 5점은 어파인(6자유도)에는 과결정이라, 어파인으로 풀면 얼굴이
찌그러진다.

선명도 게이트 임계값은 실측으로 잡았다(112×112 정렬 얼굴, 라플라시안 분산):

| 블러 반경 | 분산값 |
|---|---|
| 0px(원본) | 695 |
| 0.5px | 296 |
| 1.0px | 49 |
| 1.5px | 16 |
| 2.0px | 7 |

0.5~1.0px 사이가 절벽이라 여기를 경계로 잡았다. 인증 120 / 등록 300
(등록이 더 엄격 — 흐린 샘플이 프로필에 들어가면 이후 인증 임계값이 통째로
느슨해진다).

정렬 자체의 정확도는 **정렬된 이미지를 다시 Vision에 넣어 정준 좌표와
비교**해서 검증했다: 평균 오차 1.53~1.77px(112px 기준 1.4~1.6%).

## Phase 3 — 얼굴 인식: LFW로 실측한 오인식률

첫 모델은 InsightFace `w600k_mbf`(ArcFace, MobileFaceNet, 512차원)였다.
ONNX → PyTorch(onnx2torch) → CoreML(FP16) 변환 후 세 단계로 검증했다:

- PyTorch(FP32) vs CoreML(FP16): cosine 0.999968
- Swift 구현 vs Python 기준: cosine **1.00000000**, 최대 성분 차이 0.000000

임계값(cosine 유사도)을 감으로 정하지 않고 [LFW](http://vis.cs.umass.edu/lfw/)
(5,749명, 13,233장)를 실제 파이프라인에 통과시켜 타인 점수 분포를 쟀다:

```
실물(등록 프로필, 10샘플)  최저 0.7108, p1 0.7510, 중앙 0.8624
타인(LFW 5,675명, 12,994장) 최고 0.2780, p99.9 0.2083
분리 여유 0.4328
임계값 0.48 → FAR 0% / FRR 0%
```

일괄 처리도 **인증에 쓰는 것과 같은 코드 경로**(정렬→전처리→임베딩)로
돌렸다. 별도 스크립트로 재면 그 스크립트의 오차를 재는 꼴이 된다.

## 라이선스 문제로 모델 전면 교체 — ArcFace → SFace

배포 직전 InsightFace 모델 라이선스를 확인했다: **"모든 모델은 비상업적
연구 목적으로만 사용 가능"**. 무료 배포라도 일반 사용자 앱은 "연구"가
아니라서 위반이었다.

대체 모델 후보 두 개의 라이선스를 직접 원문 확인했다:
- dlib 얼굴 인식 모델: 저자가 퍼블릭 도메인으로 명시적 선언. 단 ONNX 변환
  경로가 없어(dlib C++ 빌드 필요) 변환 리스크가 컸다.
- OpenCV Zoo SFace: 모델 자체 LICENSE 파일이 **Apache 2.0**, 이미 ONNX로
  제공됨.

SFace로 교체했다. 정규화 방식도 검색 결과마다 답이 달라서(-127.5/128 이라는
말과, blobFromImage 가 원본값을 그대로 먹인다는 말이 상충) **ONNX 그래프를
직접 열어** 확인했다:

```
Sub(data, 127.5) → Mul(_, 0.0078125)   # 그래프 첫 두 노드
```

정규화가 모델 안에 이미 내장돼 있었다 — 즉 ArcFace 때와 반대로, Swift에서
스케일을 다시 적용하면 이중 정규화로 조용히 틀어진다. 세 겹으로 검증했다:

```
원본 ONNX vs onnx2torch 변환    cosine 0.99999988
PyTorch(FP32) vs CoreML(FP16)  cosine 0.999992
Python 파이프라인 vs Swift      cosine 1.00000000
```

실제 등록 프로필(10샘플)로 LFW를 다시 채점:

```
타인(LFW) 최고 0.3715, 실사용 본인 점수 0.93~0.94
분리 여유 0.56 — 임계값 0.48 그대로 유지
```

## Phase 4 — 안티스푸핑: 실측하지 않았으면 못 잡았을 버그 두 개

MiniFASNet(Silent-Face-Anti-Spoofing, Apache 2.0) 앙상블 두 개
(크롭 배율 2.7배/4.0배)를 쓴다.

### 버그 1 — 정답이 있는 샘플조차 위조로 판정됨

변환 직후 실물 얼굴 점수가 **0.015**로 나왔다(임계값 0.55 기준 완전 위조
판정). 크롭 배율, 채널 순서(BGR/RGB), Swift-Python 벡터 일치까지 순서대로
지웠는데도 재현됐다. 원 저장소가 배포하는 **정답 라벨이 있는 샘플**
(`image_T1.jpg`=실물, `image_F1/F2.jpg`=위조)을 그대로 넣어보니 셋 다
위조로 나왔다 — 모델 자체가 입력에 반응을 안 하는 상태였다.

원인은 전처리 코드 안에 있었다:

```python
# src/data_io/functional.py:59
# return img.float().div(255)  modify by zkx
return img.float()
```

`/255` 나눗셈이 **주석 처리**돼 있었다. 클래스 docstring에는
`[0,255] → [0.0,1.0]`이라고 적혀 있지만(torchvision에서 복사해온 문구를
그대로 둔 것), 실제 코드는 `[0,255]` 원본값을 그대로 받는다. 문서를 믿고
넘어갔으면 계속 못 잡았을 것이다 — 정답이 있는 데이터로 검증했기 때문에
잡을 수 있었다.

### 버그 2 — 위조 사진이 실제로 잠금 해제를 통과함

버그 1을 고친 뒤 폰 화면 재생 공격으로 실사용 테스트를 했는데, 몇 번은
막혔지만 **일부 시도가 실제로 통과**했다:

```
16:48:29.577 live=0.0194 smooth=0.0612   ← 위조
16:48:30.049 unlock: 인식 성공 live=0.4967   ← 통과됨
16:48:30.076 live=0.0117 smooth=0.4702      ← raw 는 위조인데 평균은 통과선 넘음
```

원인은 두 군데의 **평균 집계**였다:
1. 두 모델(2.7배/4.0배 크롭) 점수를 평균낸다 — 한 모델이 정반대로 답해도
   (`[0.017, 0.959]`) 평균 0.488로 뭉개진다.
2. 최근 5프레임을 이동평균한다 — 5장 중 2장만 실물이어도
   `(0.99×2)/5 = 0.396`으로 임계값을 넘는다.

보안 게이트에 평균을 쓰면 소수의 좋은 프레임이 나쁜 프레임을 구제해준다.
집계를 **최소값**으로 바꿨다(두 모델 중 낮은 쪽, 최근 5프레임 중 낮은 쪽 —
창이 다 찰 때까지는 판정 보류). 기존 라벨 데이터(실물 351 / 위조 96프레임)로
재검증:

```
                    실물 최소   위조 최고
평균(이전)           0.5745     0.0210
모델최소값(이후)      0.1510     0.0010   ← 위조를 20배 더 억제
```

실제 공격 프레임에 새 집계를 소급 적용하면 통과했던 두 프레임이 뒤집힌다:

```
16:48:30.076  평균 0.4702(통과) → 모델최소 0.0040(차단)
16:48:35.267  평균 0.3306(통과) → 모델최소 0.0170(차단)
```

## Phase 5 — 언락: 안전장치 세 가지

1. **주입 직전 재확인**: `CGSessionCopyCurrentDictionary()`로 잠금 상태를
   다시 확인한다. 그 사이 사용자가 직접 풀면 주입을 취소한다 — 안 그러면
   포커스된 다른 앱에 비밀번호가 그대로 타이핑된다.
2. **시도 무효화**: 매 시도에 `UUID`를 붙여 오래된 시도가 뒤늦게 실행되는
   것을 막는다.
3. **잔여 키 제거**: 노트북 뚜껑을 열 때 트랙패드/키보드 플렉스로 임의의
   키가 한 번씩 눌리는 경우가 있다. 비밀번호 입력 전 백스페이스 24번을
   먼저 보낸다 — 필드가 비어있으면 아무 일도 하지 않으므로 항상 안전하다.

## Phase 6 — 메뉴바 앱 전환에서 드러난 버그 세 개

창이 항상 떠 있던 구조에서 메뉴바 상주 앱으로 바꾸자, "창이 있다"는 암묵적
가정에 기대고 있던 버그가 한꺼번에 드러났다.

**앱 시작 시 데드락**: `AppCoordinator.init()`이 메인 스레드에서 동기적으로
키체인을 복호화했는데, 키체인이 승인 대화상자를 띄우려는 시점에 앱이 아직
UI를 만들기 전이라 대화상자를 표시할 수 없어 영원히 대기했다. 프로필 로드를
백그라운드 큐로 옮기고, 존재 여부만 확인할 때는 데이터를 읽지 않는(복호화가
없어 프롬프트도 없는) API로 분리했다.

**등록이 첫 버킷에서 영원히 멈춤**: Combine의 `@Published`는 `willSet`에서
구독자에게 새 값을 통지하는데, 그 시점엔 실제 저장값이 아직 갱신 전이다.

```swift
enrollment.$step
    .sink { [weak self] _ in
        let active = self.enrollment.isActive   // 아직 이전 값(.idle)을 읽는다
        self.camera.setEnrolling(active)         // 항상 false 로 호출됨
    }
```

등록 시작(`.idle → .collecting`) 시 `camera.setEnrolling(true)`가 로그 전체에서
**단 한 번도 호출되지 않았다.** TTA 임베딩이 계속 nil이라 첫 포즈에서
멈췄는데, 다음 `step` 전환이 없으니 버그가 스스로 고쳐질 기회도 없었다.
구독 콜백이 emit된 새 값을 직접 쓰도록 고쳤다.

**카메라가 20분 넘게 켜져 있음**: 창을 닫지 않고 최소화하면 SwiftUI의
`onDisappear`가 안 불린다 — 창은 살아있고 뷰도 마운트된 채라, 카메라를
켜 둘 이유가 계속 유지됐다. `NSWindow.didMiniaturizeNotification`을 직접
구독해 최소화 시점에 이유를 내리도록 고쳤다. 동시에 설정창·디버그창이
같은 참조 이유를 공유해 한쪽이 닫혀도 다른 쪽 때문에 안 꺼지는 구조였던
것도 창별로 분리했다.

**빌드 직후 첫 실행이 6~9초 멈춤**: 재빌드마다 macOS가 새 바이너리의
코드서명 신뢰를 처음 평가하며 몇 초 걸린다(실측 5.97~9.18초, 재실행 시
0.01초로 캐시됨). 이 비용이 화면이 잠긴 채 얼굴 인식이 성공한 순간
처음 발생하면, 진행 상황을 보여줄 수 없는 상태에서 사용자가 직접
비밀번호를 치기 시작해 주입되는 비밀번호와 뒤섞인다. 앱 시작 시점(화면이
보이는 때)에 미리 한 번 건드려 비용을 앞당겨 치르도록 했다.

## 여러 명 등록 지원에서 나온 버그

두 번째 사람을 등록하려 하면 조용히 멈췄다. 원인은 사용자별 자세 보정값
(`yawProxyNeutral`, `pitchNeutralRatio`)을 `profiles.values.first`로
아무나(Swift Dictionary 순회 순서는 안정적이지 않다) 골라 전역에 적용하고
있었던 것 — 두 번째 사람은 첫 번째 사람 기준으로 계산된 각도로 포즈 버킷
판정을 받았다. 새 등록 세션 시작 시 중립값으로 리셋하고, 등록 완료 시
"방금 등록한 그 사람"의 값만 명시적으로 적용하도록 고쳤다. 이 값은 등록 중
포즈 판정에만 쓰이고 실제 언락 판정(임베딩 유사도)에는 영향을 주지 않아,
버그가 있던 동안에도 이미 등록된 사람의 보안 자체는 영향받지 않았다.

## Phase 7 — 손바닥(손금) 인식: 원리는 되지만 아직 실용 수준이 아니다

### 왜 정맥이 아니라 손금인가

위챗페이·아마존 One은 근적외선 조명 + IR 카메라로 피부 밑 정맥을 본다.
맥북 내장 카메라는 IR 컷 필터가 달린 일반 RGB 웹캠이라 정맥이 원천적으로
안 보인다 — 소프트웨어로 우회할 수 있는 문제가 아니다. 그래서 가시광선으로
가능한 손바닥 표면 주름(팜프린트)을 쓴다.

특징 추출은 CompCode(Kong & Zhang, 2004): 6방향 Gabor 실수부 뱅크를 ROI에
돌려 픽셀마다 응답이 최소인 방향 인덱스를 코드로 삼는다. argmin만 쓰므로
밝기·대비에 원래 불변이고, 사전학습 가중치가 없어 라이선스 리스크가 0이다
(얼굴에서 ArcFace를 라이선스 때문에 버려야 했던 것과 대비된다).

### 실측: 구분은 되지만 여유가 0.02뿐

본인 양손, 각 5회 (gaborThreshold=10):

| 구분 | 점수 | 최악값 |
|---|---|---|
| 같은 손 | 0.7378 / 0.7487 / 0.7551 / 0.7665 / 0.8008 | 최저 0.7378 |
| 다른 손 | 0.6645 / 0.6702 / 0.6815 / 0.6860 / 0.7166 | 최고 0.7166 |

분리는 되지만 여유가 0.021이다. 얼굴이 타인 최고 0.3715 vs 본인 0.93으로
0.56의 여유를 갖는 것과 비교하면 25배 좁다. 이 좁은 여유가 아래 모든
문제의 뿌리다.

### 조용히 틀리던 것 세 가지

**1. "비교 불가"가 0점으로 둔갑했다**

처음엔 항상 0점이 나왔다. 원인은 `score()`가 유효 픽셀 부족을 0으로
반환한 것 — 사용자에게는 "다른 손"으로 보이지만 실제로는 "비교 자체가
성립 안 함"이었다. 두 임계값이 실측 없이 너무 빡빡했다: 응답 크기 하한 50,
그리고 유효 픽셀 요구치를 *전체 ROI의 25%*로 잡은 것. 손금 선은 원래
ROI 면적의 일부만 차지하므로 비율 기준 자체가 틀린 모델이었다.

고친 뒤: `score()`는 `Float?`를 반환해 "판단 불가"와 "낮은 점수"를 타입으로
분리. 유효 픽셀은 절대 개수(150)로, 응답 하한은 10으로.

**2. 정렬 잔차를 계산만 하고 쓰지 않았다**

`PalmAligner.diagnostics()`가 residual을 돌려주는데 매칭 경로에서 아무도
읽지 않았다. 정렬이 틀어진 프레임은 ROI가 엉뚱한 데를 잘라 CompCode가
통째로 쓰레기가 되는데, 그게 그대로 채점에 들어가 점수를 0.57까지
끌어내리고 있었다. 얼굴에는 `alignmentResidualMax`가 처음부터 있었는데
손바닥 쪽으로 옮겨오면서 빠뜨린 것이다.

**3. 튜닝값이 재시작마다 초기화됐다**

`palmFacingSign`(손바닥/손등 판별 부호)이 평범한 static var라서 앱을
재시작할 때마다 기본값으로 돌아갔다. 개발 중엔 재빌드가 잦아서 "방금
맞춰놨는데 또 안 됨"이 반복됐고, 등록 버튼이 게이트에 막혀 영영 안
눌리는 상태로 보였다. UserDefaults 백업으로 전환.

### 연속 프레임 요구치를 얼굴에서 그대로 가져온 게 실수였다

로그 실측: 같은 손을 들고 있어도 점수가 0.57~0.84로 출렁이고, 10번 중
4번만 임계값을 넘겼다(p≈0.4). 얼굴의 `requiredConsecutiveFrames=3`을 그대로
썼는데 얼굴은 여유가 커서 3연속이 쉽지만, 손바닥에서는 성공 확률이
0.4³≈6%다. 실제로 2/3까지는 갔지만 3/3은 한 번도 못 갔다. 2로 낮춰
0.4²≈16%로 올렸다 — 근본 해결이 아니라 확률 보정일 뿐이다.

### 인식률 개선: 다중 샘플 등록

참조 코드가 한 장뿐이라 등록 당시 각도에서 벗어나면 점수가 무너졌다.
얼굴은 처음부터 포즈 버킷별 다중 샘플 + 최고값 방식이었는데 손바닥만
1장이었다. `PalmProfileStore`를 `[PalmCode]`로 바꾸고 verify는 샘플 중
최고값을 쓴다.

얼굴과 다르게 중심(centroid)을 섞지 않는다 — CompCode는 방향 인덱스(0…5)라
코드끼리 평균을 내면 "중간 방향"이라는 물리적으로 무의미한 값이 나온다.
임베딩 벡터처럼 평균낼 수 있는 표현이 아니다.

### 결정적 증거 — 본인 자기일관성이 타인 점수와 겹쳐 있었다

다중 샘플 등록을 붙이고 나서야 등록 로그에서 결정적인 숫자가 나왔다. 같은 손을
같은 세션에서 몇 초 간격으로 찍은 샘플끼리의 내부일관성이 평균 0.722 / 최저
0.662였다. 앞서 잰 '다른 손'(0.6645~0.7166)과 그대로 겹친다. 무작위가 0.50,
아무 손이나 대략 0.69라는 뜻이고 — CompCode가 신원을 거의 인코딩하지 못하고
있었다. 임계값 0.73은 애초에 본인의 자기일관성보다 높아서 통과가 불가능한
값이었다. 원인 세 가지를 모두 고쳤다.

**1. 정준 좌표를 한 번도 재추정하지 않았다** (가장 큰 원인)

`canonical192`는 계획서에 "출발점 추정치, 실측으로 재추정할 것"이라고 적어둔
눈대중 값 그대로였다. 실제 손 형상과 어긋나 정렬 잔차가 상시 3.2~5.9px(게이트
6.0에 아슬아슬)였고, 매 프레임 ROI가 다른 곳을 잘랐다. `Geometry.procrustesMeanShape`
로 사용자의 실측 형상에서 좌표를 재추정한다. ROI 크롭이 정준 좌표에 의존하므로
등록을 2단계로 나눴다 — 1단계에서 5점만 모아 좌표를 확정하고, 2단계에서 그
좌표로 코드를 뽑는다. 좌표는 코드와 함께 저장한다(짝이 어긋나면 무의미해진다).

**2. 마스크 기준이 대비에 휘둘렸다**

응답의 절대 크기로 걸러서 평평한 피부도 밝기만 하면 통과했다 — 방향이 사실상
난수인 픽셀이 코드에 섞였다. 방향별 응답의 진폭(최대-최소)으로 바꿨다. "여기서
방향이 의미가 있는가"를 직접 재는 값이라 대비 변화에 둔감하다.

**3. 대비가 너무 낮았다**

웹캠으로 찍은 손바닥은 주름 대비가 낮아(validRatio 0.21~0.41) Gabor 응답의
방향차가 노이즈에 묻힌다. 루마 도메인 CLAHE를 앞단에 넣었다. 얼굴 것을 재사용하지
않았는데, 얼굴은 색 보존 때문에 강도를 낮추지만 손바닥은 색을 쓰지 않아 목표가
정반대이기 때문이다. 등록과 인증이 다른 전처리를 타는 사고를 막으려고 `encode(rgba:)`
만 노출하고 루마 진입점은 internal로 내렸다.

곁다리로, 비교 겹침 하한이 150픽셀(전체의 0.9%)이라 49개 이동 중 겹침이 희박한
이동이 요행으로 최고점을 먹고 있었다. 유효 픽셀이 적은 쪽의 35%로 바꿨다.

## Phase 8 — 초근접 손금으로 전환: 랜드마크와 손금은 양립할 수 없었다

### 먼저 카메라가 원인인지부터 확인했다

손금 인증이 성립하려면 손바닥 주름이 실제 픽셀에 찍혀야 한다. 0.5mm 주름을 2px
이상으로 담으려면 손바닥 폭이 450px 넘게 잡혀야 하고, 그러려면 손을 8~15cm까지
가져와야 한다. 그런데 맥북 내장 카메라는 대부분 고정초점이라 그 거리에서 초점이
안 맞을 수 있다 — 그렇다면 알고리즘으로는 못 넘는 벽이다. 나머지 설계가 전부 이
결과에 달려 있어서 진단 도구부터 만들었다(`tools/`, 앱 빌드와 완전히 분리).

- 8~30cm를 2cm 간격으로 `cv2.Laplacian(gray, CV_64F).var()` 측정
- 프레임 전체로 재면 배경·얼굴이 값을 지배해 거리별 비교가 무의미해지므로 중앙
  480px만 잰다
- 거리마다 12장 연사 후 최고값 채택 — 손떨림 때문에 그 거리가 부당하게 낮게
  평가되는 걸 막는다. 알고 싶은 건 달성 가능한 상한이다

실행하자마자 아이폰 카메라가 켜졌다. 근처에 아이폰이 있으면 macOS가 그걸 카메라
목록에 끼워 넣고 종종 인덱스 0(스크립트 기본값)을 차지한다. 이건 불편이 아니라
측정을 무효로 만든다 — 아이폰은 오토포커스라 접사가 잘 되므로 "근접 촬영 가능"이라는
잘못된 결론이 나오는데, 실제 앱은 맥북 내장 카메라를 쓴다. 장치를 눈으로 보고
고르는 선택 화면을 넣었다(cv2는 장치 이름을 주지 않으므로 미리보기가 유일하게
확실한 방법이다).

결과: **카메라는 문제가 아니었다.** 8~30cm 전 구간에 초점 절벽이 없었고 8cm
사진에는 손금이 육안으로 뚜렷했다. 병목은 소프트웨어 쪽이었다.

### ROI의 43%가 검은 띠였다

`roiInCanonical`(원점 32,32)로 크롭해 놓고 호출부는 (0,0)부터 렌더링하고 있었다.
32px 어긋난 영역을 읽어 L자 빈 띠 7168픽셀(43.75%)이 생겼고, 남은 부분도 손바닥
중심에서 밀려 있었다. 얼굴 쪽은 크롭·렌더 둘 다 원점 0이라 멀쩡해서, 대조하다
발견했다. `align()`이 항상 `(0,0,roiOutputSize,…)`를 돌려주도록 고치고 그 계약을
테스트로 잠갔다.

같이 드러난 것 — **128px ROI가 개인 식별 정보를 지우고 있었다.** 8cm에서 손바닥이
센서에 1280px로 잡히는데 128px로 10:1 축소하면 잔주름 그물망이 사라진다. 큰
손금(생명선 등)은 사람마다 비슷해서 그것만 남으면 구분이 안 된다 — 타인 0.69 /
본인 0.72 / 무작위 0.50이 정확히 그 상태였다. `roiOutputSize` 128 → 256,
`minSourcePixels` 160 → 450(ROI가 업샘플링되지 않을 만큼 가까이 오도록 강제).

### 성능: 4736ms → 3.0ms, 그리고 그동안 디버그 빌드로 테스트하고 있었다

256px로 올리자 인코딩 4736ms, 매칭 2763ms가 나왔다. 6방향 × 81탭 × 65536픽셀
4중 루프를 경계검사까지 하며 돌고 있었다. `vImageConvolve_PlanarF`로 교체하고
(`kvImageEdgeExtend`가 기존 clamp와 같은 동작) 매칭에서는 유효 픽셀 개수를 이동
49번마다 다시 세던 것을 루프 밖으로 뺐다.

릴리스 빌드 실측 **인코딩 3.0ms + 매칭 15.1ms = 18.2ms**.

여기서 더 중요한 걸 알았다. 같은 코드가 디버그 빌드에서는 1373ms다(75배).
`scripts/build.sh` 기본값이 debug라 그때까지 테스트한 앱이 전부 디버그였고, 대조
한 번에 1.4초씩 비전 큐를 막고 있었다. 손바닥 테스트는 release로 빌드할 것.

### 구조적 모순 — 손금이 보이는 거리에서는 랜드마크가 안 잡힌다

```
손금이 보이려면    → 손이 프레임을 꽉 채워야 함 → Vision이 손 모양을 못 봐서
                     랜드마크 없음 → 파이프라인 전체가 nil
랜드마크가 나오려면 → 손 전체가 프레임에 들어와야 함 → 너무 멀어서 손금이 안 찍힘
```

파라미터로는 절대 못 고치는 모순이었다. 실측으로도 확인했다 — 초점 스윕으로 찍은
8~30cm 근접 이미지 전부에서 `VNDetectHumanHandPose` 검출 실패, 등록 로그에도
"등록 시작" 이후 샘플이 0장이었다. ROI를 랜드마크 5점에서 뽑는 구조라, 손금이
보이는 거리에서는 아무것도 동작하지 않고 있었다.

`PalmCloseRange`를 신설해 랜드마크 없이 ROI를 잡는다. 초근접에서는 손바닥이 화면을
덮으므로 ROI를 '찾을' 필요가 없다 — 화면 중앙 정사각을 쓴다. 회전으로 생긴 빈
모서리가 들어오지 않도록 362px(=256×√2)로 받아 회전 후 중앙 256을 잘라낸다.
게이트는 살색 비율(벽·책상 걸러내기, 느슨하게)과 손금 텍스처 비율로 바꿨다.
랜드마크가 없어져 손바닥/손등 판별은 사라졌지만, 손등을 대면 코드가 안 맞아
어차피 통과하지 못한다(fail-closed).

해상도 근거: 8cm에서 손바닥 90mm가 센서에 1280px(14.2 px/mm). 중앙 정사각 720px은
손바닥 50mm 구간이고 256px로 내리면 5.1 px/mm — 0.5mm 주름이 2.6px이 되어 해상된다.
랜드마크 방식(128px, 2.1 px/mm)은 1px로 한계였다.

등록은 2단계에서 다시 1단계로 돌아갔다. 정준 좌표는 랜드마크 정렬에만 쓰던 것이라
`PalmProfile`에서도 뺐다.

### 회전 정규화를 넣었다가 실측으로 걷어냈다

전환하면서 랜드마크가 주던 회전 정규화를 구조 텐서(주름의 지배 방향)로 대신했다.
CompCode는 방향 인덱스라 15도만 밀려도 절반 칸이 틀어지기 때문이다. 곧바로 부호
버그가 하나 나왔다 — `vImageRotate_PlanarF`의 각도 방향이 구조 텐서가 재는 방향과
반대여서 `-angle`을 넘겼더니 상쇄가 아니라 누적됐다(잔여각 12도 → 25도, 25도 →
50도). 테스트로 잡았다. 놓쳤으면 "회전 정규화를 넣었는데 왜 여전히 안 되지"로
한참 헤맸을 것이다.

그런데 고치고도 "내 손금인데 불일치가 뜬다"가 계속됐다. 로그를 보니 등록 샘플끼리의
내부일관성이 **0.528** — 무작위 0.5 수준이었다. 임계값 문제가 아니라 코드가 서로
대응조차 안 되고 있었다.

**회전 정규화 자체가 이미지를 훼손하고 있었다.** 손바닥에는 서로 수직인 주름 방향이
경쟁해서 '지배 방향'이 프레임마다 뒤집힌다. 같은 사진에서 크롭 위치만 10px 옮겨도
추정값이 -3.6도 → +88.4도 → -84.8도로 튀었고, 등록 로그에서도 0.5초 간격 샘플의
회전편차가 31도였다. 그 값으로 매 프레임 다른 각도만큼 돌리면 정규화가 아니라
훼손이다. 추정을 못 믿으면 안 돌리는 게 낫다 — 걷어냈다.

**더 큰 원인은 병진이었다.** 회전이 1칸(30도) 밀려도 점수는 0.667은 나오므로 0.528이
설명되지 않는다. 초근접은 5.1 px/mm 확대라 손을 자유롭게 들고 있을 때의 2~5mm
흔들림이 10~25px 어긋남이 된다. 그런데 이동 탐색 범위가 ±3px였으니 대부분의 프레임이
범위 밖이었다. ±24px를 1px 단위로 전수 탐색하면 2401 이동이라 감당이 안 되므로
2단계로 나눴다 — 4픽셀 간격으로 성기게 훑어(비교도 4픽셀마다 하나씩, 비용 1/16)
대략을 찾고, 그 근처만 1픽셀 단위로 전체 픽셀을 본다. 릴리스 실측 16.2ms로, ±3px
전수 탐색(13.3ms)과 거의 같은 비용으로 범위를 8배 넓혔다.

### 정렬은 저주파에서, 신원은 고주파에서

"여러 장 찍어 인식률을 올리자"는 방향은 맞지만 그것만으로는 안 됐다. 등록 샘플끼리가
애초에 안 맞고 있었기 때문이다 — 2초 안에 찍은 샘플들끼리 내부일관성이 0.598~0.784로
출렁였다. 서로 안 맞는 샘플을 아무리 모아도 통과할 수 없는 템플릿이 될 뿐이다.

화각 54도 / 720p로 계산해 보니 거리마다 얻는 것과 잃는 것이 갈렸다.

| 거리 | 해상도 | 상태 |
|---|---|---|
| 6~8cm | 16~21 px/mm | 손이 프레임을 넘쳐 실루엣이 없다 = 정렬 기준이 없다 |
| 10~12cm | 10~12 px/mm | 가장자리가 보이고 해상도도 충분하다 |
| 20cm~ | 6 px/mm 이하 | 잔주름이 뭉개진다 |

즉 **너무 가까우면 오히려 인식이 안 된다.** 그동안 화면 중앙을 무조건 잘라 썼는데,
손이 어디에 얼마만 하게 있는지 모르니 매 프레임 다른 데를 자르고 있었다. 이동
탐색으로는 배율 변화를 못 잡는다.

피부 실루엣의 무게중심과 퍼진 정도로 위치·배율을 정규화하도록 바꿨다. 실루엣은 크고
저주파라 프레임마다 안정적이다 — 주름 방향(고주파)으로 정렬을 잡으려다 실패한 것과
정확히 대조된다. **정렬은 안정적인 특징에서, 신원은 고주파에서.** 손이 프레임을
넘치면 `tooClose`로 거부하고 "조금 떼세요"라고 안내한다.

등록도 바꿨다. 후보를 12장 모은 뒤 상호 점수가 0.80 이상인 것들만 골라 등록하고,
남는 게 4장 미만이면 조용히 저장하지 않고 실패시킨다. 예전에는 내부일관성 0.516짜리
템플릿이 "등록 완료"로 저장돼서, 사용자는 "등록은 됐는데 인식이 안 된다"로 겪어야
했다. 이제 그 상황이 구조적으로 불가능하고 원인도 즉시 알려준다.

### 마지막 ROI 버그 — 축소 프레임 종횡비가 정수로 절삭됐다

"인코딩되는 그림 위치가 이상하다"는 지적. 손 위치를 찾는 축소 프레임 크기를

```swift
locateSize * Int(extent.width / extent.height)
```

로 계산하고 있었는데, 1280×720에서 `Int(1.777) = 1`이라 160×160이 나왔다. 284×160
짜리 이미지를 폭 160 버퍼에 렌더링하니 프레임 **왼쪽 56%만** 보고 손 위치를
계산했고, 그래서 ROI가 엉뚱한 데로 갔다.

고치면서 좌표 변환을 `CameraController`에서 `PalmCloseRange`로 빼냈다. 여기서 두
좌표계가 만난다 — CIImage는 좌하단 원점, 렌더된 비트맵은 첫 행이 위쪽. 그 변환이
호출부에 흩어져 있으면 한쪽만 고치고 다른 쪽을 놓치기 쉽다. 한 곳에 모으고 테스트로
잠갔다. 렌더 → locate → cropRect를 실제로 태워 잘라낸 사각형이 원래 손 자리를
가리키는지 보는 테스트도 넣었다. y 뒤집기가 틀리면 위아래가 반대로 나오는데 화면에는
그럴듯한 손바닥이 떠서 눈으로는 못 알아챈다(돌려보니 y 뒤집기는 맞았고 종횡비만
문제였다).

### 튜닝 피드백 루프를 줄였다

등록이 제대로 됐는지 확인하려면 화면을 잠가봐야 했다. 실패했을 때 점수가 얼마였는지도
로그를 뒤져야 알 수 있었다. 설정 창에 실시간 인식 테스트를 넣어 일치 여부·점수·임계선을
막대로 보여준다(얼굴·손금 공통).

핵심 규칙 하나 — **점수는 잠금 해제가 쓰는 것과 완전히 같은 대조 함수에서 나온다.**
테스트용으로 따로 계산하면 그 계산의 오차를 재게 된다. 얼굴 임계값을 LFW로 잡을 때
`BatchEmbedder`를 인증과 같은 경로로 돌린 것과 같은 이유다.

얼굴이 프레임에서 사라지거나 손금이 게이트를 못 넘기면 직전 점수를 지운다. 안 그러면
손을 치웠는데도 "일치"가 그대로 떠 있어 정반대로 오해하게 된다. 계산은 해당 탭이
실제로 보일 때만 한다.

## Phase 9 — 전력: 잠금 내내 카메라를 켜 두고 있었다

`setReason(.locked, isLocked && enabled)` — 화면이 잠긴 내내 카메라를 켜 두고 있었다.
잠그고 자리를 비우면 몇 시간이고 계속 돌면서 배터리를 먹고 발열이 생기고, 녹색 LED도
상시 점등하고, 손금 파이프라인(Gabor 컨볼루션)까지 그동안 계속 돌았다. 실제로 잠금
해제가 일어나는 건 사용자가 돌아온 직후 몇 초뿐이다.

`UserPresence`를 신설해 세 신호로 '지금 앞에 있을 가능성'을 판단한다.

1. **디스플레이 잠자기**(`CGDisplayIsAsleep`). 뚜껑을 닫아도 여기로 잡힌다.
2. **마지막 입력 이후 경과 시간**(`CGEventSource.secondsSinceLastEventType`).
   잠금화면에서도 HID 이벤트는 시스템에 기록되므로 이벤트 탭 없이 읽을 수 있다 —
   권한도 필요 없고 SecurityAgent와 다투지도 않는다.
3. **디스플레이가 깨어난 순간.** 뚜껑을 열면 화면은 켜지지만 키·트랙패드 입력은 없을
   수 있다. 이걸 따로 잡지 않으면 이 앱의 대표 시나리오("뚜껑 열면 인식")가 깨진다.
   NSWorkspace 알림 + 1초 폴링 양쪽으로 잡는다.

카메라는 [잠김 && 해제 켜짐 && 앞에 있음]일 때만 켠다. 활동 후 기본 30초이고 설정에서
5~120초로 조절할 수 있다. 시간이 지나 꺼져도 뚜껑을 열거나 키·트랙패드를 건드리면 다시
켜진다. 설정 '현재' 항목에 카메라가 켜진 이유를 표시한다 — "왜 계속 켜져 있지?"를
추측하게 두면 안 된다.

### 배터리를 아끼려다 반응이 느려지면 안 된다

카메라가 꺼지고 창도 없으면 macOS가 이 앱을 App Nap으로 돌린다. 그러면 1초 타이머가
수 초~수십 초로 늘어져 "트랙패드를 건드려도 한참 뒤에야 켜지는" 문제가 생긴다.

- 잠금 해제를 기다리는 동안(잠김 && 해제 켜짐)만 `ProcessInfo.beginActivity`로 App Nap을
  막는다. `AllowingIdleSystemSleep` 옵션이라 맥이 자는 건 그대로 허용한다 — 디스플레이
  잠자기는 우리가 카메라를 끄는 신호라 막으면 안 된다.
- 같은 구간에서만 폴링을 0.4초로 촘촘히 한다. 그 외에는 2초.
- 타이머를 `.common` 런루프 모드에 넣었다. 기본 모드만 쓰면 메뉴를 여는 동안 멈춘다.

실측(2초 폴링): 입력 31초 후 presence 없음 → 트랙패드를 건드리자 0초 만에 있음 → 다시
30초 후 없음.

### 이 리팩터링이 잠금/해제를 통째로 뒤집었다 (0.2.1에서 수정)

카메라를 켤 조건이 셋으로 늘면서 흩어져 있던 판정을 `updateLockedCameraReason()`
하나로 묶었다. 그러면서 인자로 받던 값을 프로퍼티 재조회로 바꾼 게 문제였다.

```swift
- self.setReason(.locked, locked && self.unlock.isEnabled)  // emit 된 인자
+ self.updateLockedCameraReason()                            // 안에서 프로퍼티를 다시 읽음
```

`@Published` 는 **willSet 에서** 값을 흘린다. sink 안에서 원본 프로퍼티를 다시 읽으면
아직 저장 전이라 한 박자 이전 값이 온다. `isLocked` · `isEnabled` · `isPresent` 셋 다
그렇게 읽고 있었고, 결과가 정확히 반대로 나왔다.

```
12:22:49.930 lock 알림: 잠김
12:22:49.930 camera 정지 (남은 이유 없음)   ← 잠겼는데 껐다
12:22:52.045 lock 알림: 해제
12:22:52.045 camera 시작 (이유: [.locked])  ← 해제됐는데 켰다
```

presence 경로도 같이 죽어 있었다 — 자리를 비웠다 돌아와 트랙패드를 건드려도
`isPresent` 가 stale false 라 카메라가 안 켜졌다. 로그에 `presence 있음` 만 찍히고
`camera 시작` 이 따라오지 않는 것으로 남아 있었다.

고친 방식은 sink 마다 방금 emit 된 값을 인자로 넘기는 것이다. 인자가 없을 때의 기본값은
`@Published` 사본이 아니라 `ScreenLockMonitor.screenIsLocked()`(CGSession 직접 조회)로
뒀다 — willSet 타이밍과 무관하게 항상 지금의 진짜 상태다.

두 가지가 이 버그를 오래 끌게 만들었다.

- **같은 함정을 이미 한 번 겪고도 반복했다.** `enrollment.$step` sink 에 "프로퍼티를
  다시 읽으면 한 단계 이전 값을 본다"는 주석을 달아 뒀는데, 그 교훈이 코드로 강제되지
  않아 다음 리팩터링에서 그대로 재발했다.
- **로그가 전환에서만 남았다.** `setReason` 이 '카메라 켜짐↔꺼짐' 순간에만 기록해서,
  설정 창이 열려 있어 다른 이유로 이미 켜져 있으면 아무것도 안 보였다. 이유가 바뀔
  때마다 남기도록 바꾸고 나서야 로그만으로 판정이 가능해졌다.

## 손바닥 — 남은 것

- **타인 데이터셋 검증 없음.** 표본이 본인 양손뿐이다. Tongji/IITD 같은 공개 팜프린트
  세트로 얼굴의 LFW에 해당하는 채점을 해야 임계값에 근거가 생긴다. 그 전까지 0.73은
  "동작은 하게 만든 값"이지 보안 임계값이 아니다.
- **라이브니스 전무.** 등록된 손바닥 사진 한 장으로 뚫린다. MiniFASNet은 얼굴 크롭으로
  학습돼 손바닥에 전이되지 않으므로 그대로 붙일 수 없다.
- **Gabor 커널 파라미터 미조정.** 반경 4 / 시그마 2 / 파장 6 / 종횡비 0.5가 전부 출발점
  추정치다. 점수 출렁임의 상당 부분이 여기서 올 수 있다.
- **회전 보정이 없다.** 구조 텐서 방식을 걷어낸 뒤로 회전은 다루지 않는다. 필요하다면
  이미지를 미리 돌리는 대신 매칭 단계에서 다뤄야 한다 — 추정이 틀려도 되돌릴 수 있다.

## 검증 요약

| 구분 | 지표 | 값 |
|---|---|---|
| 얼굴 인식 | LFW 5,675명 타인 최고 유사도 | 0.3715 |
| 얼굴 인식 | 실사용 본인 점수 | 0.93~0.94 |
| 얼굴 인식 | 임계값 0.48 기준 FAR / FRR | 0% / 0% |
| 안티스푸핑 | 실물 하위값(모델 최소) | 0.715 |
| 안티스푸핑 | 폰 화면 위조 최고값(모델 최소) | 0.001 |
| 정렬 정확도 | 정준 좌표 대비 평균 오차 | 1.4~1.6% (112px 기준) |
| 임베딩 변환 손실 | Swift vs Python 코사인 | 1.00000000 |
| 손바닥 인코딩 | 릴리스 빌드, 256px ROI | 3.0ms (vImage 전환 전 4736ms) |
| 손바닥 매칭 | 릴리스 빌드, ±24px 2단계 탐색 | 15.1~16.2ms |
| 근접 초점 | 내장 카메라 8~30cm 라플라시안 분산 | 초점 절벽 없음 |
| 자동 테스트 | `swift test` | 30개 통과 |

## 알려진 한계

- 안티스푸핑은 **폰 화면 재생 공격만** 실측 검증했다. 인쇄물, 고해상도
  디스플레이, 동영상 재생, 3D 마스크는 검증하지 않았다.
- LFW는 '남남' 오인식만 측정한다. 일란성 쌍둥이나 의도적 분장은 다루지
  않는다.
- 로그인 비밀번호를 앱이 복원 가능한 형태로 암호화해 보관한다 — Secure
  Enclave가 지켜주는 구조가 아니다.
- Universal Binary로 빌드는 되지만 Intel Mac 실기기 테스트는 하지 못했다.
- **손바닥 인식은 실험 단계다.** 타인 데이터셋 검증이 없고 라이브니스가 전혀
  없다. 초근접 방식으로 전환하며 정렬 문제를 여럿 잡았지만(Phase 8), 임계값에
  근거를 주는 채점을 아직 못 했다는 점은 그대로다. 자세한 내용은 Phase 7~8 참고.
- 초근접 손금은 손을 **10~12cm** 에 두었을 때를 전제로 맞춰져 있다. 6~8cm 는
  해상도는 좋지만 손이 프레임을 넘쳐 정렬 기준이 사라지므로 앱이 거부한다.

## 기술 스택

Swift 6 / SwiftUI / AVFoundation / Vision / CoreML / Accelerate(vDSP) /
CryptoKit / Security(Keychain) / ServiceManagement(SMAppService) —
새 런타임 의존성 없이 Apple 프레임워크만으로 구현했다. 모델 변환에는
Python(coremltools, onnx2torch, torch)을 쓰지만 이건 빌드 시점 도구일
뿐 앱 자체의 의존성이 아니다.
